Django==6.0.6
djangorestframework==3.17.1
django-cors-headers==4.9.0
python-dotenv==1.2.2
psycopg2-binary==2.9.12
dj-database-url==3.1.2
gunicorn==26.0.0
whitenoise==6.12.0
django-redis==5.4.0
requests==2.34.2

# RAG
pgvector==0.5.0
PyYAML==6.0.2  # corpus frontmatter parsing
# Local embedding model stack (pulls torch + transformers). On Linux, install
# with the CPU-only PyTorch index to skip the multi-GB CUDA wheels:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==6.0.1
